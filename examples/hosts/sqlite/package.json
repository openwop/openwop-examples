{
  "name": "@openwop/openwop-host-sqlite",
  "version": "1.1.1",
  "private": true,
  "description": "Reference SQLite-backed OpenWOP host for conformance demos and build-your-own-host walkthroughs. Demonstrates event-log persistence and claim acquisition.",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsx test/audit-tamper.test.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
