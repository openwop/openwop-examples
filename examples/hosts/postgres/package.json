{
  "name": "@openwop/openwop-host-postgres",
  "version": "0.1.0-skeleton",
  "private": true,
  "description": "Reference Postgres-backed OpenWOP host. SKELETON as of 2026-05-11 — boots, accepts discovery, returns 501 on every other route. See README.md for the build-out plan toward the production-profile claim.",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
