{
  "name": "@openwop/openwop-host-in-memory",
  "version": "1.1.3",
  "private": true,
  "description": "Reference in-memory OpenWOP host for conformance demos and build-your-own-host walkthroughs. Minimal, single-process, and dependency-free at runtime.",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsx test/workflow-chain-expansion.test.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
