{
  "name": "@openwop/openwop-example-branch-fork",
  "version": "1.1.4",
  "private": true,
  "description": "OpenWOP branch-mode fork example: complete a parent run, fork from a chosen sequence, and observe divergent execution. Uses POST /v1/runs/{runId}:fork with mode=branch.",
  "type": "module",
  "scripts": {
    "start": "node branch-fork.mjs",
    "test": "node branch-fork.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
