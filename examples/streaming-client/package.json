{
  "name": "@openwop/openwop-example-streaming-client",
  "version": "1.1.0",
  "private": true,
  "description": "OpenWOP SSE event-stream consumer. Connects to a run's event stream and prints each event as it arrives.",
  "type": "module",
  "scripts": {
    "start": "node streaming-client.mjs",
    "test": "node streaming-client.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
