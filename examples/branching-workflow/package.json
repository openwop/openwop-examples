{
  "name": "@openwop/example-branching-workflow",
  "version": "1.1.4",
  "private": true,
  "description": "DAG-execution demo: parallel paths + fan-in via core.flow.merge. Proves the DAG scheduler runs branches concurrently.",
  "type": "module",
  "main": "branching-workflow.mjs",
  "scripts": {
    "start": "node branching-workflow.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
